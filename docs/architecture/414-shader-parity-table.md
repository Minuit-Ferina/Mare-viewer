# OpenGL / Vulkan Specialized Shader Parity Table

Generated from the current shader tree and `mare-vulkan-smoke
--list-shader-parity` on 2026-06-02.

This table tracks the specialized final Vulkan shader that should match each
OpenGL shader family. The temporary `vulkan/final/active/*` adapters are useful
for bootstrapping, but they are not the desired final performance shape when
they collapse multiple OpenGL shader families into one broad path.

Parity rule:

- OpenGL class selection remains the behavior reference.
- Vulkan should use the corresponding specialized class shader for the same
  viewer settings.
- The Vulkan shader interface must be faithful too: descriptor sets, push
  constants, varyings, vertex attributes, and material/light uniforms must
  match the runtime pipeline contract before a final shader is considered
  branchable.
- The Vulkan pipeline state must mirror the OpenGL owner state for the same
  path: blend, depth read/write, cull mode, color mask, render target, and
  pass ordering are part of shader parity.
- A row is complete only after a controlled OpenGL reference and Vulkan output
  pass image comparison, then later with viewer-like inputs.

## Specialized Parity Table

| Family / variant | OpenGL class | OpenGL reference | Final specialized Vulkan target | Current runtime status | Parity status / next step |
|---|---:|---|---|---|---|
| UI textured | 1 | `class1/interface/uiV.glsl` + `class1/interface/uiF.glsl` | `vulkan/final/class1/interface/ui.vert` + `ui.frag` | Runtime now uses the specialized `class1/interface/ui.frag`; the vertex remains `active/ui.vert` until the UI path has a faithful interface push-constant contract instead of pre-transformed clip-space positions. | Add strict UI quad reference with alpha and font texture inputs, then wire the class1 UI vertex only after the matrix/texture-matrix contract is captured and pushed. |
| UI copy | 1 | `class1/interface/copyV.glsl` + `class1/interface/copyF.glsl` | `vulkan/final/class1/interface/copy.vert` + `copy.frag` | Runtime now uses the specialized class1 interface copy vertex/fragment shaders. | Strict PPM comparison already passes with mean diff `0.0000`, max diff `0`; keep as harness baseline. |
| UI alpha mask | 1 | `class1/interface/alphamaskV.glsl` + `alphamaskF.glsl` | `vulkan/final/class1/interface/alphamask.vert` + `alphamask.frag` | Specialized target exists. | Add cutoff/discard probe separate from world alpha mask. |
| Solid color / debug UI | 1 | `class1/interface/solidcolorV.glsl` + `solidcolorF.glsl`; `debugV.glsl` + `debugF.glsl` | `vulkan/final/class1/interface/solidcolor.vert` + `solidcolor.frag`; `debug_clip.*` | Specialized target exists. | Add simple color parity probe for debug overlays and clips. |
| World simple object | 1 | `class1/objects/simpleNoAtmosV.glsl` + `class1/objects/simpleF.glsl` | `vulkan/final/class1/objects/simple_no_atmos.vert` + `simple.frag` / `simple_indexed.frag` | Runtime now binds `class1/objects/simple.frag` for direct `Textured` draws without the texture-index attribute and `class1/objects/simple_indexed.frag` for direct indexed/batched `Textured` draws. Simple offscreen G-buffer draws now use the final diffuse-indexed owner below. | Add `simple_no_atmos.vert` wiring once the world vertex transform/atmospherics contract is split from the active runtime vertex. |
| World simple no-color | 1 | `class1/objects/simpleNoColorV.glsl` + `class1/objects/simpleF.glsl` | `vulkan/final/class1/objects/simple_no_color.vert` + `simple.frag` | Specialized target exists. | Add probe with vertex color disabled. |
| World simple color | 1 | `class1/objects/simpleNoAtmosV.glsl` + `class1/objects/simpleColorF.glsl` | `vulkan/final/class1/objects/simple_no_atmos.vert` + `simple_color.frag` | Specialized target exists. | Compare vertex color multiplication and atmospheric bypass. |
| Deferred diffuse | 1 | `class1/deferred/diffuseV.glsl` + `diffuseF.glsl` | `vulkan/final/class1/deferred/diffuse.vert` + `diffuse.frag`; runtime fallback `class1/objects/world_textured_runtime.frag` | Runtime generic direct world fallback is isolated in `world_textured_runtime.frag`; specialized simple and G-buffer paths use narrower owners when selected. | Replace remaining broad fallback callsites with narrower owners, then add controlled G-buffer color/normal/depth comparison. |
| Deferred diffuse indexed | 1 | `class1/deferred/diffuseV.glsl` + `diffuseIndexedF.glsl` | `vulkan/final/class1/deferred/diffuse.vert` + `diffuse_indexed.frag`; `diffuse_indexed.vert` + `diffuse_indexed_gbuffer*.frag` | Runtime simple `Textured` G-buffer now binds `diffuse_indexed.vert` plus explicit 3-attachment and 4-attachment final G-buffer fragments. | Verify texture-index selection and descriptor indexing against an OpenGL reference capture. |
| Deferred diffuse alpha mask | 1 | `class1/deferred/diffuseV.glsl` + `diffuseAlphaMaskF.glsl` / `diffuseAlphaMaskIndexedF.glsl` | `vulkan/final/class1/deferred/diffuse_indexed.vert` + `diffuse_alpha_mask_indexed.frag`; runtime owner `diffuse_alpha_mask_runtime.frag` | Runtime AlphaMask now has separate direct and G-buffer owners. Direct/post-deferred AlphaMask binds `diffuse_alpha_mask_runtime.frag`; G-buffer AlphaMask binds the indexed final class1 pair with the world push-constant/texture-index/skinning ABI. The G-buffer alpha-mask fragments now use the OpenGL `GBUFFER_FLAG_HAS_ATMOS` value and matching normal encoding instead of writing `normal.a = 1.0`. | Viewer-pipeline scene capture `gbuffer-normal-alpha` is strict zero-diff after the fix: `diff max 0`, mean `0.0`, changed pixels `0 / 1281840`. Add controlled direct cutoff/discard references with alpha texture next. |
| Deferred diffuse alpha mask indexed | 1 | `class1/deferred/diffuseV.glsl` + `diffuseAlphaMaskIndexedF.glsl` | `vulkan/final/class1/deferred/diffuse.vert` + `diffuse_alpha_mask_indexed.frag` | Specialized target exists and shares the corrected OpenGL-style G-buffer normal flag/encoding. | Covered by the viewer-pipeline scene `PASS_ALPHA_MASK` `gbuffer-normal-alpha` capture; add an independent texture-index cutoff probe. |
| Deferred diffuse no-color alpha mask | 1 | `class1/deferred/diffuseNoColorV.glsl` + `diffuseAlphaMaskNoColorF.glsl` | `vulkan/final/class1/deferred/diffuse_no_color.vert` + `diffuse_alpha_mask_no_color.frag` | Specialized target exists and shares the corrected OpenGL-style G-buffer normal flag/encoding. | Add no-color alpha mask probe with cutoff/discard and normal-alpha comparison. |
| Terrain legacy | 1 | `class1/deferred/terrainV.glsl` + `terrainF.glsl` | `vulkan/final/class1/deferred/terrain.vert` + `terrain.frag`; runtime owners `terrain_runtime.frag` and `terrain_gbuffer*.frag` | Runtime terrain direct and G-buffer fragments are now isolated under class1/deferred while keeping the current `active/terrain.vert` ABI. | Compare splat/base-color texture blending and G-buffer writes, then branch the specialized terrain vertex interface once the UBO/varying contract is captured. |
| Terrain PBR | 1 | `class1/deferred/pbrterrainV.glsl` + `pbrterrainF.glsl` | `vulkan/final/class1/deferred/pbrterrain.vert` + `pbrterrain.frag`; runtime G-buffer owner `terrain_gbuffer*.frag` | Runtime PBR terrain G-buffer shares the final class1 terrain G-buffer fragments with GLTF factors, paint maps, texture transforms, ORM, emissive, and normal maps. The vertex side still uses the current runtime terrain ABI. | Compare terrain material channels and `pbrterrain_util_f.glsl`, then split true PBR terrain vertex/pipeline owners when the final interface is ready. |
| Tree | 1 | `class1/deferred/treeV.glsl` + `treeF.glsl` | `vulkan/final/class1/deferred/tree.vert` + `tree.frag` | Specialized target exists. | Add tree alpha/cutoff probe; keep separate from generic alpha. |
| Deferred bump | 1 | `class1/deferred/bumpV.glsl` + `bumpF.glsl` | `vulkan/final/class1/deferred/bump.vert` + `bump.frag` | Specialized target exists. Runtime Vulkan currently routes deferred bump through the Material G-buffer owner with a `LegacyBump` material flag, restricted to actual Bump/PostBump command classes so ordinary legacy materials do not inherit bump env/specular behavior. | Viewer-pipeline scene `gbuffer-normal` now matches OpenGL env/flag output for the bump probe; remaining RGB delta is max `[1, 1, 0]` from normal quantization only. |
| Object/Post bump | 1 | `class1/objects/bumpV.glsl` + `bumpF.glsl` | `vulkan/final/class1/objects/bump.vert` + `bump.frag` | Specialized target exists; the PostBump contract now exposes OpenGL `BT_MULT_X2` as Vulkan `MultiplyX2` with read-only depth. | Keep direct object bump separate from deferred bump and verify multiply-x2 composition. |
| Fullbright | 1 | `class1/deferred/fullbrightV.glsl` + `fullbrightF.glsl` | `vulkan/final/class1/deferred/fullbright.vert` + `fullbright.frag`; runtime owner `fullbright_runtime.frag` | Runtime fullbright now binds `class1/deferred/fullbright_runtime.frag` with the existing world vertex/push-constant ABI and OpenGL alpha blending. The runtime fragment now matches OpenGL `fullbrightF.glsl` by converting world fullbright color from sRGB to linear before writing into the deferred screen target. The specialized class1 shader remains inventory-only until its UBO/G-buffer interface is connected. | Viewer-pipeline scene `deferred-light` capture is now near zero-diff after the sRGB-to-linear fix and synthetic-camera restoration: max RGB diff `[1, 1, 1]`, mean diff effectively `0`, changed pixels `9 / 1281840`. Next: add non-shiny fullbright probe with real texture/cutoff variants, then split direct/fullbright G-buffer ownership from the runtime owner. |
| Fullbright shiny | 1/3 | `class1/deferred/fullbrightShinyV.glsl` + `class3/deferred/fullbrightShinyF.glsl` | `vulkan/final/class1/deferred/fullbright_shiny.vert` + `vulkan/final/class3/deferred/fullbright_shiny.frag`; runtime owner `fullbright_runtime.frag` | Runtime fullbright-shiny currently shares the fullbright runtime owner. The specialized shiny shader exists but is not bound at runtime. | Verify shiny reflection/specular path separately from plain fullbright before branching the specialized owner. |
| Emissive | 1 | `class1/deferred/emissiveV.glsl` + `emissiveF.glsl` | `vulkan/final/class1/deferred/emissive.vert` + `emissive.frag` | Specialized target exists. The viewer-pipeline fixture now validates both emissive contracts: the default absent-buffer case captures strict black parity, and `MARE_VIEWER_PIPELINE_SCENE_TEST_ENABLE_EMISSIVE_BUFFER=1` allocates attachment 3 plus a nonzero PBR emissive probe. Vulkan PBR G-buffer emissive now matches OpenGL by treating the material emissive factor as already linear and only converting the emissive texture sample from sRGB. | Current nonzero `gbuffer-emissive` probe is strict zero-diff: max channel diff `0`, changed channels `0`. Add mapped-emissive-texture and legacy-material emissive probes next. |
| Legacy material class1 | 1 | `class1/deferred/materialV.glsl` + `materialF.glsl` | `vulkan/final/class1/deferred/material.vert` + `material.frag` | Specialized target exists, but the current OpenGL class1 fragment in this tree is a debug stub. Runtime material G-buffer uses the class3-derived owner below. | Keep class1 as an inventory target until a real class1 OpenGL material reference exists. |
| Legacy material class3 | 3 | `class1/deferred/materialV.glsl` + `class3/deferred/materialF.glsl` | `vulkan/final/class1/deferred/material.vert` + `vulkan/final/class3/deferred/material.frag` / `material_runtime.frag` / `material_gbuffer*.frag` | Runtime legacy Material now has separate direct and G-buffer owners: direct/post-deferred binds `class3/deferred/material_runtime.frag`, while deferred G-buffer binds `class3/deferred/material_gbuffer*.frag` with the current Vulkan world push-constant ABI. `LegacyBump` is no longer inferred for every nonzero `mBump` material draw; it is restricted to Bump/PostBump command classes so Material/SpecMap/NormMap keep the OpenGL `env_intensity * spec.a` normal.z contract. | Viewer-pipeline scene captures after the G-buffer capture-point fix: `gbuffer-depth` strict zero-diff, `gbuffer-normal-alpha` strict zero-diff, and `gbuffer-normal` max `[1, 1, 0]` / mean `[0.140656, 0.140656, 0.0]` from 8-bit normal XY quantization. Replace the runtime material ABI with the specialized class3 interface after the forward/alpha material pass has a faithful final descriptor and push-constant contract. |
| PBR opaque deferred | 1 | `class1/deferred/pbropaqueV.glsl` + `pbropaqueF.glsl` | `vulkan/final/class1/deferred/pbropaque.vert` + `pbropaque.frag` / `pbr_runtime.frag` / `pbropaque_gbuffer*.frag` | Runtime PBR now has separate direct and G-buffer owners: direct/post-deferred binds `class1/deferred/pbr_runtime.frag`, while deferred G-buffer binds `class1/deferred/pbropaque_gbuffer*.frag` with the current Vulkan world push-constant ABI. The PBR emissive G-buffer owner now preserves OpenGL's material-factor/color-space contract for attachment 3. | Nonzero PBR emissive G-buffer fixture passes strict OpenGL/Vulkan image parity. Replace the runtime PBR ABI with the specialized class1 interface after the forward/HUD PBR path has a faithful final descriptor and push-constant contract, then compare base color, normal, ORM, mapped emissive textures, and reflection-probe terms. |
| PBR alpha class1 | 1 | `class1/deferred/pbralphaV.glsl` + `pbralphaF.glsl` | `vulkan/final/class1/deferred/pbralpha.vert` + `pbralpha.frag` | Specialized target exists. | Add alpha blend/mask PBR probe. |
| PBR alpha class2 | 2 | `class1/deferred/pbralphaV.glsl` + `class2/deferred/pbralphaF.glsl` | `vulkan/final/class1/deferred/pbralpha.vert` + `vulkan/final/class2/deferred/pbralpha.frag` | Specialized target exists. | Compare class2 reflection-probe influence and alpha. |
| PBR glow | 1 | `class1/deferred/pbrglowV.glsl` + `pbrglowF.glsl` | `vulkan/final/class1/deferred/pbrglow.vert` + `pbrglow.frag` | Specialized target exists. | Keep glow PBR separate from legacy glow combine. |
| GLTF metallic roughness | 1 | `class1/gltf/pbrmetallicroughnessV.glsl` + `pbrmetallicroughnessF.glsl` | `vulkan/final/class1/gltf/pbrmetallicroughness.vert` + `pbrmetallicroughness.frag` | Specialized target exists. | Compare GLTF material path independently from viewer PBR opaque. |
| Avatar direct | 1 | `class1/avatar/avatarV.glsl` + `avatarF.glsl` | `vulkan/final/class1/avatar/avatar.vert` + `avatar.frag` / `avatar_runtime.frag` | Runtime direct avatar now binds `class1/avatar/avatar_runtime.frag` with the current Vulkan world push-constant ABI. The specialized class1 avatar shader remains inventory-only until the final avatar interface is connected. | Add direct avatar texture probe, then replace the runtime avatar ABI with the specialized class1 interface. |
| Avatar skinning include | 1 | `class1/avatar/avatarSkinV.glsl`; `objectSkinV.glsl` | `vulkan/final/class1/avatar/avatar_skin_v.glsl`; `object_skin_v.glsl` | Specialized includes exist. | Add skinned vertex-buffer probe before judging avatar parity. |
| Avatar deferred | 1 | `class1/deferred/avatarV.glsl` + `avatarF.glsl` | `vulkan/final/class1/deferred/avatar.vert` + `avatar.frag`; runtime owners `avatar_gbuffer_runtime.frag` and `avatar_gbuffer_emissive_runtime.frag` | Runtime avatar G-buffer is isolated from the final class1 avatar inventory shader while preserving the current world push-constant/skinning ABI. | Compare baked avatar texture, material color, and G-buffer output, then replace the runtime avatar G-buffer ABI with the specialized class1 interface. |
| Avatar eyes | 1 | `class1/avatar/eyeballV.glsl` + `eyeballF.glsl`; `class1/deferred/avatarEyesV.glsl` | `vulkan/final/class1/avatar/eyeball.vert` + `eyeball.frag`; `class1/deferred/avatar_eyes.vert` | Specialized target exists. | Add eyeball-specific UV and lighting probe. |
| Avatar shadows | 1 | `avatarShadowV/F.glsl`; `avatarAlphaShadowV/F.glsl`; `avatarAlphaMaskShadowF.glsl` | `avatar_shadow.*`; `avatar_alpha_shadow.*`; `avatar_alpha_mask_shadow.frag` | Specialized target exists. | Compare avatar shadow caster variants separately. |
| Avatar velocity | 1 | `avatarVelocityV.glsl` + `avatarVelocityF.glsl` | `avatar_velocity.vert` + `avatar_velocity.frag` | Specialized target exists. | Verify motion-vector output if velocity is enabled. |
| Alpha post-water | 1/2 | `class1/deferred/alphaV.glsl` + `class2/deferred/alphaF.glsl` | `vulkan/final/class1/deferred/alpha.vert` + `vulkan/final/class2/deferred/alpha.frag` | Runtime now uses `class1/deferred/alpha.vert` and `class2/deferred/alpha.frag`; the vertex keeps the current Vulkan world push-constant/skinning contract and the fragment uses an OpenGL-style linear legacy Alpha lighting path. The pipeline contract uses `ForwardAlpha`, separate from standard `BT_ALPHA`, so destination alpha attenuation matches `LLDrawPoolAlpha`. | OpenGL source-level reference exists and compiles both vertex/fragment with `USE_VERTEX_COLOR`. Current strict RGB comparison is mean diff `5.3333`, max diff `15`; Vulkan alpha readback is nonzero (`0.2784` average in the smoke probe). Next: add real local-light/reflection/fog inputs and align depth/post-water ordering. |
| PBR shadow alpha blend | 1 | `class1/deferred/pbrShadowAlphaBlendF.glsl` | `vulkan/final/class1/deferred/pbr_shadow_alpha_blend.frag` | Specialized target exists. | Add shadow alpha blend probe. |
| PBR shadow alpha mask | 1 | `class1/deferred/pbrShadowAlphaMaskV.glsl` + `pbrShadowAlphaMaskF.glsl` | `vulkan/final/class1/deferred/pbr_shadow_alpha_mask.vert` + `pbr_shadow_alpha_mask.frag` | Specialized target exists. | Compare PBR shadow mask cutoff. |
| Sky | 1 | `class1/deferred/skyV.glsl` + `skyF.glsl` | `vulkan/final/class1/deferred/sky.vert` + `sky.frag`; runtime owner `sky_runtime.frag` | Runtime sky now binds `class1/deferred/sky_runtime.frag` with the existing world vertex/push-constant ABI. The faithful `sky.vert`/`sky.frag` pair remains inventory-only until the sky UBO, rainbow/halo/HDRI texture bindings, and OpenGL varyings are connected. | Add a dedicated sky pipeline/interface contract before replacing the runtime owner with the faithful class1 sky pair. |
| Clouds | 1 | `class1/deferred/cloudsV.glsl` + `cloudsF.glsl` | `vulkan/final/class1/deferred/clouds.vert` + `clouds.frag` | Specialized target exists. | Add cloud texture/noise probe. |
| Stars | 1 | `class1/deferred/starsV.glsl` + `starsF.glsl` | `vulkan/final/class1/deferred/stars.vert` + `stars.frag` | Specialized target exists. | Add night-sky/star probe. |
| Sun disc | 1 | `class1/deferred/sunDiscV.glsl` + `sunDiscF.glsl` | `vulkan/final/class1/deferred/sun_disc.vert` + `sun_disc.frag` | Specialized target exists. | Compare sun color/intensity and screen position. |
| Moon | 1 | `class1/deferred/moonV.glsl` + `moonF.glsl` | `vulkan/final/class1/deferred/moon.vert` + `moon.frag` | Specialized target exists. | Compare moon texture and alpha. |
| Water class1 | 1 | `class1/environment/waterV.glsl` + `waterF.glsl` | `vulkan/final/class1/environment/water.vert` + `water.frag`; runtime owner `water_runtime.frag` | Runtime water now binds `class1/environment/water_runtime.frag` with the existing world vertex/push-constant ABI and depth/scene-color/water-exclusion inputs. The class1 target remains the OpenGL magenta fallback shader, not the final visible water path. | Compare normals, fresnel, fog, reflection, refraction, then add a dedicated water interface before replacing the runtime owner. |
| Water class3 | 3 | `class1/environment/waterV.glsl` + `class3/environment/waterF.glsl` | `vulkan/final/class1/environment/water.vert` + `vulkan/final/class3/environment/water.frag` | Specialized target exists but is not bound at runtime; current runtime uses `water_runtime.frag`. | Keep class3 water separate from class1 water and branch it only after the real water UBO/texture contract is available. |
| Underwater | 3 | `class3/environment/underWaterF.glsl` | `vulkan/final/class3/environment/under_water.frag` | Specialized target exists. | Add underwater fog/refraction probe. |
| Water haze | 3 | `class3/deferred/waterHazeV.glsl` + `waterHazeF.glsl` | `vulkan/final/class3/deferred/water_haze.vert` + `water_haze.frag` | Specialized target exists. | Compare water haze post pass separately from sky haze. |
| Haze | 2/3 | `class2/deferred/softenLightV.glsl` + `class3/deferred/hazeF.glsl` | `vulkan/final/class2/deferred/soften_light.vert` + `vulkan/final/class3/deferred/haze.frag`; runtime owner `haze_runtime.frag` | Runtime haze now binds `class3/deferred/haze_runtime.frag` by default instead of requiring `MARE_VULKAN_ENABLE_APPROXIMATE_HAZE`. The runtime owner receives the same atmospheric `DeferredSoften` UBO and composite push-constant contract as the soften pass, samples screen coordinates from the fullscreen varyings like OpenGL `softenLightV`, reconstructs view position from scene depth, and applies an OpenGL-derived `calcAtmosphericVarsLinear()` path. Water haze still uses the older runtime approximation. | OpenGL source-level reference exists. `shader-probe --shader-case haze` and `viewer-deferred-haze-probe` now compare against the real OpenGL `softenLightV/hazeF` source helper with the Haze blend state; strict PPM is close but red with mean diff `1.3333`, max diff `2` (`5,6,8` OpenGL versus `4,5,6` Vulkan), and both pass with `--compare-ppm-mean-tolerance 2 --compare-ppm-max-tolerance 4`. The remaining gap is a uniform 8-bit direct-to-swapchain/reference quantization difference, not the earlier black/mismatched Haze path. Next: replace the runtime owner with the final class3 haze interface and add captured/live-scene water-exclusion plus water-haze validation. |
| Soften/deferred composite | 2/3 | `class2/deferred/softenLightV.glsl` + `class3/deferred/softenLightF.glsl` | `vulkan/final/class3/deferred/soften_light.vert` + `soften_light.frag`; fallback owner `deferred_composite_runtime.frag` | Runtime now has a dedicated `DeferredSoften` owner bound to `class3/deferred/soften_light.frag` for the viewer-style composite graph. The shader consumes G-buffer color/specular/normal/emissive, depth, lightMap, lightFunc, environment/probe textures, SSR scene inputs, reflection-probe UBO, and the OpenGL-derived soften/atmosphere state. The fallback `deferred_composite_runtime.frag` remains only as an approximation path. | `viewer-deferred-soften-state-probe` exercises the live `DeferredSoften` owner with synthetic legacy/material/PBR G-buffer bands, non-neutral lightMap, classic moon-selected lighting, probe inputs, SSR state, and final handoff; current targeted smoke passes. Vulkan now mirrors OpenGL's `GBUFFER_FLAG_HAS_HDRI` and `GBUFFER_FLAG_SKIP_ATMOS` early branches instead of sending those pixels through legacy lighting. Source-reference probes exist for `soften-skip-atmos`/`viewer-deferred-soften-skip-atmos-probe`, `soften-legacy`/`viewer-deferred-soften-legacy-probe`, `soften-legacy-emissive`/`viewer-deferred-soften-legacy-emissive-probe`, `soften-pbr-emissive`/`viewer-deferred-soften-pbr-emissive-probe`, `soften-pbr-brdf`/`viewer-deferred-soften-pbr-brdf-probe`, `soften-pbr-probe`/`viewer-deferred-soften-pbr-probe`, `soften-pbr-ssr`/`viewer-deferred-soften-pbr-ssr-probe`, `soften-legacy-specular`/`viewer-deferred-soften-legacy-specular-probe`, `soften-legacy-lightmap`/`viewer-deferred-soften-legacy-lightmap-probe`, and `soften-legacy-env`/`viewer-deferred-soften-legacy-env-probe`; they compile the real OpenGL `softenLightV/F.glsl` sources and compare against the live Vulkan `DeferredSoften` owner. Current strict PPM diff is `0.0000` mean, `0` max for SKIP_ATMOS/emissive, neutral legacy diffuse, legacy diffuse with nonzero emissive ignored like OpenGL, PBR emissive-only, legacy environment mix, PBR radiance/irradiance probe lighting, and the controlled PBR SSR sceneMap/sceneDepthMap mix. PBR non-classic BRDF/punctual lighting passes with mean `0.0095`, max `1`; legacy specular/lightFunc passes with mean `0.0193`, max `1`; legacy lightMap shadow+SSAO consumption passes with mean `0.3333`, max `1`. Next: extend SSR validation from the constant source-reference scene to moving-camera/viewer captures. |
| Sun light / lightMap shadows | 2 | `class2/deferred/sunLightV.glsl` + `sunLightF.glsl` + `class1/deferred/shadowUtil.glsl` | `vulkan/final/class2/deferred/sun_light.vert` + `sun_light.frag`; runtime owner `vulkan/final/class2/deferred/sun_light_map_runtime.frag` | Runtime `DeferredLightMap` now binds `sun_light_map_runtime.frag` and transports real sun/spot shadow-map inputs through the Vulkan lightMap UBO/push-constant contract. The final `sun_light.frag` inventory shader is still not the bound runtime owner. | Source-reference probe exists: `mare-vulkan-smoke --shader-case lightmap-shadow --opengl-reference-ppm <path>` plus `--mode viewer-deferred-lightmap-shadow-probe --compare-ppm <opengl.ppm>` compares the live Vulkan `DeferredLightMap` output against OpenGL source shaders. Current strict PPM diff is `0.0000` mean, `0` max; PCF edge sampling now clamps shadow XY like OpenGL `CLAMP_TO_EDGE`. Next: decide whether to split this runtime owner into the inventory `sun_light.frag` interface or keep the runtime lightMap ABI as the final specialized owner. |
| Sun light SSAO / lightMap AO | 2 | `class2/deferred/sunLightV.glsl` + `sunLightSSAOF.glsl` + `class1/deferred/aoUtil.glsl` | `vulkan/final/class2/deferred/sun_light.vert` + `sun_light_ssao.frag`; runtime owner `vulkan/final/class2/deferred/sun_light_map_runtime.frag` | Runtime `DeferredLightMap` now computes the SSAO channel from synthetic-comparable depth/normal/noise inputs. The final `sun_light_ssao.frag` inventory shader is still not the bound runtime owner. | Source-reference probe exists: `mare-vulkan-smoke --shader-case lightmap-ssao --opengl-reference-ppm <path>` plus `--mode viewer-deferred-lightmap-ssao-probe --compare-ppm <opengl.ppm>` compares the live Vulkan `DeferredLightMap` SSAO channel against OpenGL `sunLightSSAOF/aoUtil`. Current strict PPM diff is `0.0000` mean, `0` max after readback-origin normalization; Vulkan sample position reconstruction now matches OpenGL's unclamped screen-coordinate path. Next: validate viewer-like depth/normal inputs and the post-blur consumer path. |
| LightMap blur | 2 | `class1/deferred/blurLightV.glsl` + `blurLightF.glsl` | `vulkan/final/class1/deferred/blur_light.vert` + `blur_light.frag`; runtime owner `vulkan/final/class1/deferred/blur_light_runtime.frag` | Runtime `DeferredBlurLight` now runs the OpenGL-equivalent two-pass lightMap ping-pong blur before `DeferredSoften`, using the transported blur kernel, direction, screen size, and normal/depth inputs. The final inventory pair remains separate from the runtime ABI. | Source-reference probe exists: `mare-vulkan-smoke --shader-case lightmap-blur --opengl-reference-ppm <path>` plus `--mode viewer-deferred-lightmap-blur-probe --compare-ppm <opengl.ppm> --compare-ppm-max-tolerance 16`. Current diff is mean `0.0957`, max `13`, localized at synthetic band/upscale transitions. Next: validate live scene softness and depth/normal edge rejection. |
| Point light | 3 | `class3/deferred/pointLightV.glsl` + `pointLightF.glsl` | `vulkan/final/class3/deferred/point_light.vert` + `point_light.frag` | Runtime has a dedicated `PointLight` cube-volume owner and a separate `MultiPointLight` fullscreen-batch owner for nearby lights, matching the OpenGL split instead of folding local light energy into `DeferredSoften`. | Smoke probes `viewer-deferred-point-light-volume-probe` and `viewer-deferred-local-light-probe` validate additive light-target output plus final handoff against stable RGB expectations. Next: add OpenGL source-reference or live-scene comparison for attenuation/specular energy. |
| Multi point light | 3 | `class3/deferred/multiPointLightV.glsl` + `multiPointLightF.glsl` | `vulkan/final/class3/deferred/multi_point_light.vert` + `multi_point_light.frag` | Runtime `MultiPointLight` fullscreen owner is wired for nearby point-light batches with transient `set=1` uniforms, G-buffer/depth/lightFunc inputs, and class-tier deferred shader level from `LLViewerShaderMgr`. | `viewer-deferred-local-light-probe` covers the fullscreen batch path. Next: compare multi-light accumulation/permutation behavior against OpenGL in a controlled source-reference or captured scene. |
| Spot light class1 | 1 | `class1/deferred/spotLightF.glsl` | `vulkan/final/class1/deferred/spot_light.frag` | Runtime `SpotLight` cube-volume owner is wired, with spot/projector uniforms, projector matrices, texture handles, lightFunc, and `DeferredLightMap` spot-shadow inputs. | `viewer-deferred-spot-light-volume-probe` validates the cube-volume projector path. Next: compare class1 projected/no-shadow spot output against OpenGL source imagery. |
| Spot light class3 | 3 | `class3/deferred/spotLightF.glsl` | `vulkan/final/class3/deferred/spot_light.frag` | Class3 spot shader is preserved separately and selected from the deferred shader level rather than from a Vulkan-only quality heuristic. | Runtime class-tier selection is wired; remaining work is class3 PBR/shadowed spot visual comparison against OpenGL. |
| Multi spot class1/class2 | 1/2 | `class1/deferred/multiSpotLightF.glsl`; `class2/deferred/multiSpotLightF.glsl` | `vulkan/final/class1/deferred/multi_spot_light.frag`; `class2/deferred/multi_spot_light.frag` | Runtime `MultiSpotLight` fullscreen owner is wired for nearby projectors, with current projector candidate/index/fade ownership transported from the viewer pipeline. | `viewer-deferred-projector-light-probe` validates fullscreen projector output and final handoff. Next: compare class1/class2 multi-spot permutations and projected texture/shadow softness against OpenGL. |
| Reflection probe class2 | 2 | `class2/deferred/reflectionProbeF.glsl`; `class2/interface/reflectionprobeV/F.glsl` | `vulkan/final/class2/deferred/reflection_probe_f.glsl`; `class2/interface/reflectionprobe.*` | Vulkan has native `TextureCubeMapArray` allocation/copy support for reflection, irradiance, and hero probe layers; `DeferredSoften` binds the cube arrays plus `ReflectionProbeData` UBO. | Smoke probes `viewer-deferred-reflection-probe`, `viewer-deferred-real-reflection-probe`, and `viewer-deferred-hero-probe` guard fallback, cube-array descriptor, and hero-probe mix paths. Next: validate live viewer-managed reflection/irradiance arrays and class2 alpha/water consumers. |
| Reflection probe class3 | 3 | `class3/deferred/reflectionProbeF.glsl` | `vulkan/final/class3/deferred/reflection_probe_f.glsl` | `DeferredSoften` now carries a first Vulkan port of class3 probe influence/neighbor selection, parallax, manual/automatic weighting, max LOD, hero blend, and SSR mix behavior. | Source-reference probes now cover PBR radiance/irradiance and a controlled SSR mix. Next: compare parallax/probe selection in populated live scenes and water/debug variants. |
| Screen-space reflections | 3 | `class3/deferred/screenSpaceReflPostV.glsl` + `screenSpaceReflPostF.glsl` | `vulkan/final/class3/deferred/screen_space_refl_post.vert` + `screen_space_refl_post.frag`; integrated path in `soften_light.frag` | Integrated `DeferredSoften` SSR uses OpenGL-derived glossy Poisson raymarching with `sceneMap`, `sceneDepthMap`, SSR parameters, and inverse modelview delta. The standalone post shader remains an inventory target. | `viewer-deferred-ssr-probe` and `soften-pbr-ssr`/`viewer-deferred-soften-pbr-ssr-probe` validate deterministic SSR inputs with strict source-reference diff `0.0000`, max `0`. Next: moving-camera/captured-scene SSR and standalone post-buffer parity. |
| Shadow opaque | 1 | `class1/deferred/shadowV.glsl` + `shadowF.glsl` | `vulkan/final/class1/deferred/shadow.vert` + `shadow.frag` | Specialized target exists. | Compare depth/shadow-map writes. |
| Shadow alpha mask | 1 | `class1/deferred/shadowAlphaMaskV.glsl` + `shadowAlphaMaskF.glsl` | `vulkan/final/class1/deferred/shadow_alpha_mask.vert` + `shadow_alpha_mask.frag` | Specialized target exists. | Verify cutoff in shadow map. |
| Shadow cube/skinned | 1 | `shadowCubeV.glsl`; `shadowSkinnedV.glsl` | `shadow_cube.vert`; `shadow_skinned.vert` | Specialized target exists. | Add cube and skinned shadow probes if used by Vulkan path. |
| Glow extract | 1 | `class1/effects/glowExtractV.glsl` + `glowExtractF.glsl` | `vulkan/final/class1/effects/glow_extract.vert` + `glow_extract.frag` | Specialized target exists. | Split from glow combine. |
| Glow combine | 1 | `class1/effects/glowV.glsl` + `glowF.glsl`; `class1/interface/glowcombineV/F.glsl` | `vulkan/final/class1/effects/glow.vert` + `glow.frag`; `class1/interface/glowcombine.*`; runtime owner `glow_runtime.frag` | Runtime world glow draw commands now bind `class1/effects/glow_runtime.frag` and keep OpenGL `BT_ADD` (`one / one`) state. The specialized glow extract/combine shaders remain separate post-process targets, not replacements for the world glow draw-command shader. | Compare world glow draw output separately from extract and combine outputs. |
| FXAA | 1 | `class1/deferred/fxaaF.glsl`; `class1/interface/glowcombineFXAAV/F.glsl` | `vulkan/final/class1/deferred/fxaa.frag`; `class1/interface/glowcombine_fxaa.*` | Specialized target exists. | Verify post-AA toggles and final color. |
| SMAA | 1 | `SMAA*.glsl` | `smaa.glsl`; `smaa_edge_detect.*`; `smaa_blend_weights.*`; `smaa_neighborhood_blend.*` | Specialized target exists. | Compare each SMAA stage independently. |
| DoF combine | 1 | `class1/deferred/dofCombineF.glsl` | `vulkan/final/class1/deferred/dof_combine.frag` | Specialized target exists. | Add DoF disabled/enabled final composite probes. |
| Post deferred base | 1 | `postDeferredV.glsl` + `postDeferredF.glsl` | `vulkan/final/class1/deferred/post_deferred.vert` + `post_deferred.frag`; runtime owner `final_composite_runtime.frag` | Runtime final composite is isolated in `class1/deferred/final_composite_runtime.frag`. It remains the bootstrap post adapter until the post-deferred variants are wired. | Split no-tonemap, tonemap, gamma, DoF, AA variants. |
| Post deferred no DoF | 1 | `postDeferredV.glsl` + `postDeferredNoDoFF.glsl` | `vulkan/final/class1/deferred/post_deferred.vert` + `post_deferred_no_dof.frag` | Specialized target exists. | Compare with DoF disabled. |
| Post deferred gamma | 1 | `postDeferredGammaCorrect.glsl` | `vulkan/final/class1/deferred/post_deferred_gamma.frag` | Specialized target exists. | Compare gamma-only output. |
| Post deferred tonemap | 1 | `postDeferredTonemap.glsl` | `vulkan/final/class1/deferred/post_deferred_tonemap.frag` | Specialized target exists. | Compare tone mapping against OpenGL settings. |
| Buffer visualize | 1 | `postDeferredVisualizeBuffers.glsl` | `vulkan/final/class1/deferred/post_deferred_visualize_buffers.frag` | Specialized target exists. | Keep as debug-only parity aid. |
| Occlusion | 1 | `class1/interface/occlusionV.glsl` + `occlusionF.glsl`; cube/skinned variants | `vulkan/final/class1/interface/occlusion.*`; `occlusion_cube.vert`; `occlusion_skinned.vert` | Specialized target exists. | Compare query/occlusion rendering separately from visible color path. |

## Performance Intent

The final Vulkan renderer should prefer specialized pipelines over broad shader
supersets:

1. Simple textured objects should not pay material, PBR, alpha, or deferred
   lighting costs.
2. Class1, class2, and class3 shader selection should mirror the OpenGL viewer
   settings.
3. Expensive fragment paths such as material, PBR, water, haze, SSAO, lights,
   SSR, glow, and final composite should stay separated.
4. Shared vertex logic is acceptable only when the vertex format and uniforms
   are genuinely identical.
5. Temporary `active/*` adapters should be treated as bootstrap owners until
   the specialized final pipeline is bound and tested.

## Current Priority

1. Keep `LLWorldRenderCommand::get_world_render_pipeline_contract()` and
   `mare-vulkan-smoke --list-pipeline-contracts` as the pre-shader contract
   guardrail. A final Vulkan shader is not branchable until its ABI and selected
   Vulkan pipeline reproduce that OpenGL-derived material contract.
2. Keep `Copy` as the strict zero-diff harness baseline.
3. Promote `Haze` from the current runtime owner to the final specialized
   class2/class3 graph now that direct and offscreen viewer-style smoke probes
   exist; the remaining probe gap is live/captured water-exclusion and
   water-haze behavior rather than basic AtmosphericHaze wiring.
4. Split broad `Textured`, `Fullbright`, `Alpha`, `Glow`, and `FinalComposite`
   probes into the specialized variants above.
5. Bind runtime Vulkan pipelines to these specialized targets only after each
   variant has a probe, a clear OpenGL reference, a matching shader interface,
   and a matching pipeline-state contract.
6. Keep the viewer-pipeline scene test camera explicit while debugging the real
   deferred graph. The test intentionally uses a synthetic orthographic camera;
   after Vulkan fullscreen composite passes, post-deferred 3D overlays must
   restore that synthetic camera instead of calling the normal viewer
   `setup3DRender()` path. Long term this is render-graph debt: Vulkan deferred
   passes should carry explicit per-pass matrices/state rather than depending
   on global `setup2DRender()` / `setup3DRender()` side effects.
